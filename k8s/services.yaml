apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: dummyrequestcounter-service
spec:
  type: NodePort
  selector:
    app: dummyrequestcounter
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
